---
import Icon from '../Icon.astro';
import SectionHeader from '../SectionHeader.astro';
import DashPreview from '../previews/DashPreview.astro';
import CheckoutTemplate from '../previews/CheckoutTemplate.astro';
import ReconcileTemplate from '../previews/ReconcileTemplate.astro';
import OnboardTemplate from '../previews/OnboardTemplate.astro';

const rows = [
  {
    icon: 'cog',
    tone: '',
    title: 'Guided Account Setup',
    text: 'We walk you through opening your own merchant account with Stripe, Adyen or Checkout.com — no jargon, no paperwork headaches.',
    Preview: DashPreview,
  },
  {
    icon: 'sparkles',
    tone: 'green',
    title: 'Branded Checkout Pages',
    text: "Bespoke payment pages that match your company's look and feel — professional, on-brand and buyer-friendly.",
    Preview: CheckoutTemplate,
  },
  {
    icon: 'refresh',
    tone: 'amber',
    title: 'Automated Reconciliation',
    text: 'Every card payment is matched to your invoices and records automatically — no more chasing bank transfers.',
    Preview: ReconcileTemplate,
  },
  {
    icon: 'users',
    tone: 'purple',
    title: 'Fully Managed Onboarding',
    text: 'We handle the entire setup and walk your team through it — no technical know-how required.',
    Preview: OnboardTemplate,
  },
];
---
<section class="value-prop section-pad" id="about" data-screen-label="Value props">
  <div class="container">
    <SectionHeader
      eyebrow="Why SettlePay"
      title="We Build the Bridge to "
      blue="Effortless Payments"
      subtitle="We handle every step — from setting up your merchant account to building your branded checkout page — so you never chase a bank transfer again."
    />
    <div class="bento">
      {rows.map((r) => {
        const Preview = r.Preview;
        return (
          <article class="bento-card" data-reveal>
            <div class={'bento-card__icon' + (r.tone ? ' bento-card__icon--' + r.tone : '')}>
              <Icon name={r.icon} sw={1.5} />
            </div>
            <h3 class="bento-card__title">{r.title}</h3>
            <p class="bento-card__text">{r.text}</p>
            <div class="bento-card__media">
              <Preview />
            </div>
          </article>
        );
      })}
    </div>
  </div>
</section>
