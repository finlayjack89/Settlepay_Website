# Stripe TEST publishable key (pk_test_…) — enables real Stripe test mode in the Marsh & Vale demo; leave empty for the built-in mock.
PUBLIC_STRIPE_TEST_PUBLISHABLE_KEY=

# URL of the deployed PaymentIntent Worker (see serverless/stripe-demo/) — leave empty to run the demo in validate-only mode.
PUBLIC_STRIPE_DEMO_ENDPOINT=
